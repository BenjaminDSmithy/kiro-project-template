# Web App Override

Use this file when working inside `apps/web/`.

- Run web-app checks before broader monorepo checks when both are available.
- Prefer changes scoped to `apps/web/` unless the task explicitly requires shared package edits.
- Document route, auth, or design-system behavior changes in the web-app docs before handoff.
