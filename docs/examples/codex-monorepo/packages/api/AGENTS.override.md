# API Package Override

Use this file when working inside `packages/api/`.

- Treat schema and contract changes as release-relevant until proven otherwise.
- Prefer request or event contract tests when behavior changes.
- Call out migrations, backfills, or compatibility notes explicitly in the handoff.
